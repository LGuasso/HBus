package br.com.expressobits.hbus.utils;

import android.graphics.Bitmap;

/**
 * Created by rafael on 18/10/15.
 */
public class ImageUtils {

    /**
     * Puxa e converte imagem para bitmap
     * @param medic
     * @return
     *
     * TODO implmentar parse lib
    private static Bitmap getBitmapImage(File) throws ParseException {
        Bitmap bitmap = null;
        ParseFile file = (ParseFile)medic.get(Medic.PROFILE_PICTURE);

        if(file !=null){
            byte[] bites = file.getData();
            bitmap = BitmapFactory.decodeByteArray(bites, 0, bites.length);
        }

        return bitmap;
    */

}
